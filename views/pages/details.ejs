<!DOCTYPE html>
<html>

<head>
  <title></title>
  <link rel="stylesheet" href="" type="text/html">
</head>


<body>
  <main>
    <h1><%= render.name %></h1>
    <h2><%= render.description %></h2>
    <% render.photo.forEach(photo => { %>
    <img src="<%= photo.request.url %>" alt="">
    <% }) %>
  </main>
</body>

</html>